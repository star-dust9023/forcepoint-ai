# Ask AI contained RAG service — runtime dependencies.
# Install into a venv:  pip install -r ask_service/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.40
httpx>=0.27
pydantic>=2.6
